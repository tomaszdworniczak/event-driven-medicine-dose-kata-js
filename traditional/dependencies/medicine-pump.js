function MedicinePump() {
    return {
        dose: function (medicine) {
            // we don't care what the implementation of this method is
        },
        getTimeSinceLastDoseInMinutes: function (medicine) {
            // we don't care what the implementation of this method is
        }
    }
}